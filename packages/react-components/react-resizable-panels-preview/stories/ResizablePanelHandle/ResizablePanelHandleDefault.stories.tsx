import * as React from 'react';
import { ResizablePanelHandle, ResizablePanelHandleProps } from '@fluentui/react-resizable-panels-preview';

export const Default = (props: Partial<ResizablePanelHandleProps>) => <ResizablePanelHandle {...props} />;
