import * as React from 'react';
import { ResizablePanel, ResizablePanelProps } from '@fluentui/react-resizable-panels-preview';

export const Default = (props: Partial<ResizablePanelProps>) => <ResizablePanel {...props} />;
